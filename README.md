# Complex Bash Project
A complex, production-ready Bash project with multiple layers and interdependencies

## Run the project
1. Build the Docker image: `docker build -t complex-bash-project .`
2. Run the Docker container: `docker run complex-bash-project`